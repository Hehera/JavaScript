1.Масиви об'єктів

Масиви об’єктів є найпоширенішим типом даних, тому що більшість API  повертають масиви об’єктів.
Знаючи, що змінна — це масив, ми можемо отримати доступ до властивостей масивів через крапкову нотацію і викликати методи масиву.

1.1 Перетворення масивів об'єктів(map,filter,find,some,every)
1.1 Незміність
1.2 Глибоке порівняння двух об'єктів,масивів (===)

1.3 Операції з незмінними масивами
 Незмінний об'єкт - це той, який не можна змінити.Ми не можемо змінювати незмінний об’єкт, ми створюємо повністю новий об’єкт на основі оригіналу, залишаючи вихідний об’єкт без змін.

// Строге порівняння
// Якщо ви хочете порівняти за значеннями ===.

// Глибоке порівняння
// Якщо ви хочете порівняти два об'єкти або масиви
    
2.Деструктирізація масивів - дозволяє брати значення з масиву та присвоювати їх змінним.



